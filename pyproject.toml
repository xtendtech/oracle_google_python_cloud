[project]
name = "oracle-google-python-cloud"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "oci>=2.163.1",
]

[tool.uv.workspace]
members = [
    "oracle",
]
